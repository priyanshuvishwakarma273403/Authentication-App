server:
  port: 8083

  spring:
    datasource:
      url: jdbc:mysql://localhost:3306/auth_app_yt
      username: root
      password: pop99558$$

      jpa:
        hibernate:
          ddl-auto: update
        properties:
          hibernate:
            dialect: org.hibernate.dialect.MySQLDialect
            format_sql: true
        show-sql: true